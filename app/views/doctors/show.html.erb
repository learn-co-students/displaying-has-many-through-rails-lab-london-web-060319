 <h1> <%= @doctor.name%> </h1>
<p> <% @doctor.appointments.each do |appointment|%> </p>
 <%= appointment.appointment_datetime.strftime("%B %-d, %Y at %k:%M")%>
<p> <%= link_to appointment.patient.name, patient_path(appointment.patient)%> <p>
<%end%> 